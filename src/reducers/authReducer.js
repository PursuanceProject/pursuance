const initialState = {
  authenticated: true,
  username: 'elimisteve'  
};

export default function (state = initialState, action) {
  
    switch (action.type) {

      default:
        return state
    }
  
  }
  